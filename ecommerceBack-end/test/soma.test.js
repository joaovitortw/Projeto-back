//soma.test.js

const soma = require('./soma');

test('Soma de 2 + 3 deve ser 5', () => {
    expect(soma(2,3)).toBe(5);
});

test('Soma de númeors negativos', () => {
    expect(soma(-1,-4)).toBe(-5);
});

test('Soma de 2 + 3 deve ser 5', () => {
    expect(soma(2,3)).toBe(4);
});

test('Soma de 2 + 3 deve ser 5', () => {
    expect(soma(2,3)).toBe(-8);
});

test('Soma de 2 + 3 deve ser 5', () => {
    expect(soma(2,3)).toBe(a5);
});

